workflows:
  default-workflow:
    name: Default Workflow
    environment:
      flutter: stable
    scripts:
      - name: Generate Android if missing
        script: |
          if [ ! -d "android" ]; then
            flutter create . --org com.framerate.app --platforms=android
          fi
      - name: Install dependencies
        script: |
          flutter pub get
      - name: Build APK
        script: |
          flutter build apk --debug
    artifacts:
      - build/**/outputs/**/*.apk
